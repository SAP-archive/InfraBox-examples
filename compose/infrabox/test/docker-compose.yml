version: "2"

services:
    server:
        build:
            context: ../../
            dockerfile: infrabox/server/Dockerfile
        volumes:
            - "${INFRABOX_PATH}:/infrabox"

    test:
        build:
            context: ../../
            dockerfile: infrabox/test/Dockerfile
        links:
            - server
        container_name: compose_test_server
